%p
  %b= I18n.t 'project.products'
  %ul
    - project.line_items.each do |item|
      %li
        %span.currency= number_to_currency(item.price, :precision => 2, :unit => "€", :format => "%n %u")
        %span -- 
        %span= item.units 
        %span x  
        %span= item.product.name
        %span --
        %span= link_to I18n.t('shared.edit'), edit_project_line_item_path(project, item)
        %span \|
        %span= link_to I18n.t('shared.delete'), [project, item], method: :delete, data: { confirm: 'Are you sure?' }