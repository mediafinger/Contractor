%h1 Editing line item

= render 'form', :line_item => @line_item, :project => @project, :products => @products

= render 'project_items', :project => @project

= link_to 'Show', @project
\|
= link_to 'Back', project_path(@project)
